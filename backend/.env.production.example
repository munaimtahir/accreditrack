# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=your-google-cloud-ip,localhost,127.0.0.1

# Database Configuration
DB_NAME=accreditrack
DB_USER=accreditrack
DB_PASSWORD=your-secure-database-password
DB_HOST=db
DB_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://your-google-cloud-ip,https://your-domain.com

# Security Settings (set to True when SSL is configured)
SECURE_SSL_REDIRECT=False

# Optional: Email Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
